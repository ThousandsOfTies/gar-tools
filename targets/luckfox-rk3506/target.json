{
  "id": "luckfox-rk3506",
  "displayName": "Luckfox Lyra Plus (RK3506)",
  "description": "Luckfox Lyra Plus physical target running RK3506/Buildroot (armv7l) with BusyBox init, real GPIO/SPI devices, and SSH deployment.",
  "toolsRoot": "targets/luckfox-rk3506",
  "compatibility": {
    "architecture": "armv7l",
    "abi": "gnueabihf",
    "libc": "glibc",
    "toolchainTriple": "arm-buildroot-linux-gnueabihf"
  },
  "defaultBackends": {
    "codespace": "local",
    "simulator": "ssh_remote",
    "target": "ssh_scp"
  },
  "backendNotes": {
    "simulator": "Use the linux-device runtime on an ARM64 SSH simulation host.",
    "target": "Deploy to the physical Lyra Plus over an SSH config Host. The board runs Buildroot with BusyBox init rather than systemd.",
    "media": "Use the RK3506 Buildroot media stack available in the selected board image.",
    "ui": "Drive the physical ILI9341 and KY-040 through the board's real SPI and GPIO devices; simulation stubs are not deployed to the target.",
    "prepare": "Run gar target prepare once to install the constrained Buildroot deploy helper and BusyBox init template."
  },
  "provisioning": {
    "ssh_scp": {
      "type": "ssh-script",
      "path": "provisioning/buildroot-busybox",
      "recipeVersion": 1,
      "lifecycle": {
        "type": "gar-app-lifecycle-v1",
        "command": "/usr/local/lib/gar/gar-target-lifecycle"
      }
    }
  }
}
